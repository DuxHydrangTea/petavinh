import 'package:flutter/material.dart';

class PostInsideGroup extends StatelessWidget {
  const PostInsideGroup({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
